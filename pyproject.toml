[project]
name = "local-llm-chat"
version = "0.1.0"
description = "A small, local-only chat interface for Ollama"
readme = "README.md"
license = "MIT"
requires-python = ">=3.11"
dependencies = []

[tool.uv]
package = false
